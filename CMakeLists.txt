CMAKE_MINIMUM_REQUIRED(VERSION 3.3)

PROJECT( hao_prom_metrics CXX )

SET(CMAKE_CXX_FLAGS "-std=c++17 ${CMAKE_CXX_FLAGS}")

ADD_EXECUTABLE( ${PROJECT_NAME} main.cpp )

TARGET_LINK_LIBRARIES(${PROJECT_NAME} curl)
